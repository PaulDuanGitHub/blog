<% if (category) { %>
    <a
        href="javascript:void(0)"
        onclick="jumpToArchives('category', '<%= category.name %>')"
        data-category="<%= category.name %>">
        <%= category.name %>
    </a>
<% } %>